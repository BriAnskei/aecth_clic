<ContentDialog
    x:Class="aesth_clic.Views.Roles.SuperAdmin.Modals.ReactivateClient"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Reactivate Client"
    PrimaryButtonText="Yes, Reactivate"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <StackPanel Spacing="16" Width="420">

        <!-- Icon + Heading -->
        <StackPanel HorizontalAlignment="Center" Spacing="12">
            <Border
                Width="72" Height="72"
                CornerRadius="36"
                Background="#1A0EA47A"
                HorizontalAlignment="Center">
                <FontIcon
                    Glyph="&#xE72C;"
                    FontSize="32"
                    Foreground="#0EA47A"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" />
            </Border>

            <TextBlock
                x:Name="ClientNameText"
                Text="Client Name"
                FontSize="16"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"
                TextAlignment="Center"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        </StackPanel>

        <!-- Divider -->
        <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <!-- Confirmation message -->
        <StackPanel Spacing="8">
            <TextBlock
                Text="Are you sure you want to reactivate this client account?"
                FontSize="13"
                TextWrapping="Wrap"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}" />

            <TextBlock
                Text="The client will regain access to the system immediately using their existing credentials."
                FontSize="12"
                TextWrapping="Wrap"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Info strip -->
        <Border
            Background="#1A0EA47A"
            BorderBrush="#400EA47A"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12,10">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <FontIcon
                    Glyph="&#xE783;"
                    FontSize="14"
                    Foreground="#0EA47A"
                    VerticalAlignment="Top" />
                <TextBlock
                    Text="All previous data, settings, and assigned modules will be restored for this client."
                    FontSize="12"
                    TextWrapping="Wrap"
                    Foreground="#0EA47A" />
            </StackPanel>
        </Border>

    </StackPanel>
</ContentDialog>

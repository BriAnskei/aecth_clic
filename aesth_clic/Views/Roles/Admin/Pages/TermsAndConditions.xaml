<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="aesth_clic.Views.Roles.Admin.Pages.TermsAndConditions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:aesth_clic.Views.Roles.Admin.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F7F4FD">

    <Page.Resources>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background"      Value="White" />
            <Setter Property="BorderBrush"     Value="#E4DAF5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius"    Value="12" />
            <Setter Property="Padding"         Value="24,20" />
        </Style>

        <Style x:Key="TncTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#2D1554" />
        </Style>

        <Style x:Key="TncDescStyle" TargetType="TextBlock">
            <Setter Property="FontSize"     Value="13" />
            <Setter Property="Foreground"   Value="#7A6A9A" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin"       Value="0,5,0,0" />
            <Setter Property="LineHeight"   Value="20" />
        </Style>

        <Style x:Key="CardTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#2D1554" />
        </Style>

        <Style x:Key="CardSubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="12" />
            <Setter Property="Foreground" Value="#9B80C4" />
            <Setter Property="Margin"     Value="0,2,0,0" />
        </Style>

        <Style x:Key="ClinicTncCardStyle" TargetType="Border">
            <Setter Property="Background"      Value="#FDFBFF" />
            <Setter Property="BorderBrush"     Value="#E4DAF5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius"    Value="8" />
        </Style>

        <Style x:Key="EditTitleBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize"      Value="14" />
            <Setter Property="FontWeight"    Value="SemiBold" />
            <Setter Property="MaxLength"     Value="120" />
            <Setter Property="BorderBrush"   Value="#C9A8E8" />
            <Setter Property="CornerRadius"  Value="6" />
            <Setter Property="Padding"       Value="10,6" />
        </Style>

        <Style x:Key="EditDescBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize"      Value="13" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping"  Value="Wrap" />
            <Setter Property="MinHeight"     Value="100" />
            <Setter Property="BorderBrush"   Value="#C9A8E8" />
            <Setter Property="CornerRadius"  Value="6" />
            <Setter Property="Padding"       Value="10,8" />
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="#7B3FCC" />
            <Setter Property="Foreground"      Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CornerRadius"    Value="6" />
            <Setter Property="Padding"         Value="16,8" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="FontWeight"      Value="SemiBold" />
        </Style>

        <Style x:Key="SubtleButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="Transparent" />
            <Setter Property="BorderBrush"     Value="#C9A8E8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius"    Value="6" />
            <Setter Property="Padding"         Value="14,7" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="Foreground"      Value="#5B2D8E" />
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="#FEF2F2" />
            <Setter Property="Foreground"      Value="#DC2626" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CornerRadius"    Value="6" />
            <Setter Property="Padding"         Value="14,7" />
            <Setter Property="FontSize"        Value="13" />
        </Style>

        <!-- Skeleton block placeholder -->
        <Style x:Key="SkeletonBlockStyle" TargetType="Border">
            <Setter Property="Background"        Value="#EDE4F9" />
            <Setter Property="CornerRadius"      Value="4" />
            <Setter Property="Height"            Value="14" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

    </Page.Resources>

    <!-- Root grid so ToastBar can float over the page -->
    <Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Padding="28,24,28,32" RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- ══════════════════════════════════
                     ROW 0 · Page Header
                ══════════════════════════════════ -->
                <StackPanel Grid.Row="0" Spacing="2" Margin="0,0,0,4">
                    <TextBlock
                        Text="Terms and Conditions"
                        FontSize="26"
                        FontWeight="Bold"
                        Foreground="#2D1554" />
                    <TextBlock
                        Text="Review and manage the clinic's terms and conditions."
                        FontSize="13"
                        Foreground="#9B80C4" />
                </StackPanel>

                <!-- ══════════════════════════════════
                     ROW 1 · Info Banner
                ══════════════════════════════════ -->
                <Border
                    Grid.Row="1"
                    Background="#EDE4F9"
                    BorderBrush="#C9A8E8"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="16,12">
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE946;" FontSize="16" Foreground="#5B2D8E" VerticalAlignment="Center" />
                        <TextBlock
                            Text="The system provisions below are set by the system administrator and are read-only. You may add and manage your own clinic-specific provisions in the section below."
                            FontSize="12"
                            Foreground="#5B2D8E"
                            TextWrapping="Wrap"
                            VerticalAlignment="Center" />
                    </StackPanel>
                </Border>

                <!-- ══════════════════════════════════
                     ROW 2 · System TNC Card (read-only)
                ══════════════════════════════════ -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                    <StackPanel Spacing="0">

                        <!-- Card header -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="2">
                                <TextBlock Text="System Terms and Conditions" Style="{StaticResource CardTitleStyle}" />
                                <TextBlock Text="Set by system administrator · Read-only" Style="{StaticResource CardSubtitleStyle}" />
                            </StackPanel>
                            <Border
                                Grid.Column="1"
                                Background="#EDE4F9"
                                CornerRadius="6"
                                Padding="10,4"
                                VerticalAlignment="Center">
                                <TextBlock
                                    x:Name="TxtMasterEntryCount"
                                    Text="0 provisions"
                                    FontSize="12"
                                    Foreground="#5B2D8E"
                                    FontWeight="SemiBold" />
                            </Border>
                        </Grid>

                        <Border Height="1" Background="#F0EAF8" Margin="0,0,0,20" />

                        <!-- ── MASTER SKELETON ── -->
                        <StackPanel x:Name="MasterSkeletonList" Spacing="16" Visibility="Visible">
                            <StackPanel Spacing="10">
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="180" HorizontalAlignment="Left" />
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="420" HorizontalAlignment="Left" Height="10" />
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="380" HorizontalAlignment="Left" Height="10" />
                            </StackPanel>
                            <Border Height="1" Background="#F0EAF8" />
                            <StackPanel Spacing="10">
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="210" HorizontalAlignment="Left" />
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="400" HorizontalAlignment="Left" Height="10" />
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="360" HorizontalAlignment="Left" Height="10" />
                            </StackPanel>
                            <Border Height="1" Background="#F0EAF8" />
                            <StackPanel Spacing="10">
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="160" HorizontalAlignment="Left" />
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="440" HorizontalAlignment="Left" Height="10" />
                                <Border Style="{StaticResource SkeletonBlockStyle}" Width="390" HorizontalAlignment="Left" Height="10" />
                            </StackPanel>
                        </StackPanel>

                        <!-- ── MASTER REAL LIST ── -->
                        <StackPanel x:Name="MasterTncList" Spacing="0" Visibility="Collapsed" />

                    </StackPanel>
                </Border>

                <!-- ══════════════════════════════════
                     ROW 3 · Clinic-Specific TNC Card
                ══════════════════════════════════ -->
                <Border Grid.Row="3" Style="{StaticResource CardStyle}">
                    <StackPanel Spacing="0">

                        <!-- Card header -->
                        <Grid x:Name="ClinicCardToolbar" Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
                                <TextBlock Text="Clinic-Specific Terms" Style="{StaticResource CardTitleStyle}" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE8A9;" FontSize="11" Foreground="#9B80C4" VerticalAlignment="Center" />
                                    <TextBlock x:Name="TxtEntryCount" Text="0 entries" Style="{StaticResource CardSubtitleStyle}" VerticalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>

                            <Button
                                x:Name="BtnAddNew"
                                Grid.Column="1"
                                Click="BtnAddNew_Click"
                                Style="{StaticResource SaveButtonStyle}"
                                VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE710;" FontSize="12" Foreground="White" VerticalAlignment="Center" />
                                    <TextBlock Text="Add Provision" FontSize="13" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </Grid>

                        <!-- Divider -->
                        <Border Height="1" Background="#F0EAF8" Margin="0,0,0,16" />

                        <!-- ── CLINIC SKELETON ── -->
                        <StackPanel x:Name="ClinicSkeletonList" Spacing="10" Visibility="Visible">
                            <Border Style="{StaticResource ClinicTncCardStyle}" Padding="16,14">
                                <StackPanel Spacing="10">
                                    <Border Style="{StaticResource SkeletonBlockStyle}" Width="200" HorizontalAlignment="Left" />
                                    <Border Style="{StaticResource SkeletonBlockStyle}" Width="380" HorizontalAlignment="Left" Height="10" />
                                    <Border Style="{StaticResource SkeletonBlockStyle}" Width="340" HorizontalAlignment="Left" Height="10" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource ClinicTncCardStyle}" Padding="16,14">
                                <StackPanel Spacing="10">
                                    <Border Style="{StaticResource SkeletonBlockStyle}" Width="170" HorizontalAlignment="Left" />
                                    <Border Style="{StaticResource SkeletonBlockStyle}" Width="400" HorizontalAlignment="Left" Height="10" />
                                    <Border Style="{StaticResource SkeletonBlockStyle}" Width="360" HorizontalAlignment="Left" Height="10" />
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- ── CLINIC REAL LIST ── -->
                        <StackPanel x:Name="ClinicTncList" Spacing="10" Visibility="Collapsed" />

                    </StackPanel>
                </Border>

            </Grid>
        </ScrollViewer>

        <!-- Floating ToastBar -->
        <InfoBar
            x:Name="ToastBar"
            IsOpen="False"
            IsClosable="True"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch"
            Margin="28,8,28,0" />

    </Grid>
</Page>

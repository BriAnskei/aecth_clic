<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="aesth_clic.Views.Roles.Receptionist.Pages.ServiceMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:aesth_clic.Views.Roles.Receptionist.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F7F4FD">

    <Page.Resources>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background"      Value="White" />
            <Setter Property="BorderBrush"     Value="#E4DAF5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius"    Value="12" />
            <Setter Property="Padding"         Value="20" />
        </Style>

        <Style x:Key="KpiValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="30" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="#2D1554" />
        </Style>

        <Style x:Key="KpiLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize"         Value="11" />
            <Setter Property="FontWeight"       Value="SemiBold" />
            <Setter Property="Foreground"       Value="#9B80C4" />
            <Setter Property="CharacterSpacing" Value="50" />
        </Style>

        <Style x:Key="KpiSubStyle" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="12" />
            <Setter Property="Foreground" Value="#B0A0CC" />
            <Setter Property="Margin"     Value="0,3,0,0" />
        </Style>

        <Style x:Key="ColHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="10" />
            <Setter Property="FontWeight"        Value="SemiBold" />
            <Setter Property="Foreground"        Value="#9B80C4" />
            <Setter Property="CharacterSpacing"  Value="50" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="CellTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="13" />
            <Setter Property="Foreground"        Value="#2D1554" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming"      Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="CellSecondaryStyle" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="12" />
            <Setter Property="Foreground"        Value="#9B80C4" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming"      Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="ToolbarComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background"          Value="White" />
            <Setter Property="Foreground"          Value="#2D1554" />
            <Setter Property="BorderBrush"         Value="#C4A8E8" />
            <Setter Property="BorderThickness"     Value="1.5" />
            <Setter Property="CornerRadius"        Value="8" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment"   Value="Center" />
            <Setter Property="Padding"             Value="12,8" />
            <Setter Property="FontSize"            Value="13" />
        </Style>

    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Padding="28,24,28,32" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- ROW 0 · Page Header -->
            <Grid Grid.Row="0">
                <StackPanel Spacing="2">
                    <TextBlock
                        Text="Service Menu"
                        FontSize="26" FontWeight="Bold"
                        Foreground="#2D1554" />
                    <TextBlock
                        Text="Browse all procedures and services offered by the clinic"
                        FontSize="13" Foreground="#9B80C4" />
                </StackPanel>
            </Grid>

            <!-- ROW 1 · KPI Cards -->
            <Grid Grid.Row="1" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Total Services -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel Spacing="0">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,12">
                            <Border Width="36" Height="36" CornerRadius="8" Background="#EDE4F9">
                                <FontIcon Glyph="&#xE9D9;" FontSize="16" Foreground="#5B2D8E" />
                            </Border>
                            <TextBlock Text="TOTAL SERVICES" Style="{StaticResource KpiLabelStyle}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock x:Name="TxtTotalServices" Text="0" Style="{StaticResource KpiValueStyle}" />
                        <TextBlock Text="All available procedures" Style="{StaticResource KpiSubStyle}" />
                    </StackPanel>
                </Border>

                <!-- Lowest Price -->
                <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                    <StackPanel Spacing="0">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,12">
                            <Border Width="36" Height="36" CornerRadius="8" Background="#E8F5E9">
                                <FontIcon Glyph="&#xE8EC;" FontSize="16" Foreground="#2E7D32" />
                            </Border>
                            <TextBlock Text="STARTING FROM" Style="{StaticResource KpiLabelStyle}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock x:Name="TxtLowestPrice" Text="₱0" Style="{StaticResource KpiValueStyle}" />
                        <TextBlock Text="Lowest procedure price" Style="{StaticResource KpiSubStyle}" />
                    </StackPanel>
                </Border>

                <!-- Highest Price -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel Spacing="0">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,12">
                            <Border Width="36" Height="36" CornerRadius="8" Background="#FFF3E0">
                                <FontIcon Glyph="&#xE8EC;" FontSize="16" Foreground="#E65100" />
                            </Border>
                            <TextBlock Text="UP TO" Style="{StaticResource KpiLabelStyle}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock x:Name="TxtHighestPrice" Text="₱0" Style="{StaticResource KpiValueStyle}" />
                        <TextBlock Text="Highest procedure price" Style="{StaticResource KpiSubStyle}" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ROW 2 · Search + Filter Toolbar -->
            <Border
                Grid.Row="2"
                Background="White"
                BorderBrush="#E4DAF5"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16,12">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="260" />
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Search by procedure name -->
                    <TextBox
                        Grid.Column="0"
                        x:Name="SearchBox"
                        PlaceholderText="Search procedures…"
                        VerticalAlignment="Center"
                        Background="White"
                        Foreground="#2D1554"
                        PlaceholderForeground="#9B80C4"
                        BorderBrush="#C4A8E8"
                        BorderThickness="1.5"
                        CornerRadius="8"
                        Padding="12,8"
                        FontSize="13"
                        TextChanged="SearchBox_TextChanged">
                        <TextBox.Resources>
                            <SolidColorBrush x:Key="TextBoxFocusedBorderBrush"     Color="#5B2D8E" />
                            <SolidColorBrush x:Key="TextBoxPointerOverBorderBrush" Color="#9B80C4" />
                        </TextBox.Resources>
                    </TextBox>

                    <!-- Filter by Doctor -->
                    <ComboBox
                        Grid.Column="1"
                        x:Name="DoctorFilter"
                        Style="{StaticResource ToolbarComboBoxStyle}"
                        SelectionChanged="DoctorFilter_SelectionChanged">
                        <ComboBox.Resources>
                            <SolidColorBrush x:Key="ComboBoxFocusedBorderBrush"     Color="#5B2D8E" />
                            <SolidColorBrush x:Key="ComboBoxPointerOverBorderBrush" Color="#9B80C4" />
                        </ComboBox.Resources>
                        <ComboBoxItem Content="All Doctors" Tag="All" IsSelected="True" />
                    </ComboBox>

                    <!-- Filter by Price Range -->
                    <ComboBox
                        Grid.Column="2"
                        x:Name="PriceFilter"
                        Style="{StaticResource ToolbarComboBoxStyle}"
                        SelectionChanged="PriceFilter_SelectionChanged">
                        <ComboBox.Resources>
                            <SolidColorBrush x:Key="ComboBoxFocusedBorderBrush"     Color="#5B2D8E" />
                            <SolidColorBrush x:Key="ComboBoxPointerOverBorderBrush" Color="#9B80C4" />
                        </ComboBox.Resources>
                        <ComboBoxItem Content="All Prices"      Tag="All"   IsSelected="True" />
                        <ComboBoxItem Content="Under ₱500"      Tag="U500" />
                        <ComboBoxItem Content="₱500 – ₱2,000"  Tag="500to2000" />
                        <ComboBoxItem Content="₱2,000 – ₱5,000" Tag="2000to5000" />
                        <ComboBoxItem Content="Above ₱5,000"   Tag="A5000" />
                    </ComboBox>

                    <!-- Spacer -->
                    <Grid Grid.Column="3" />
                </Grid>
            </Border>

            <!-- ROW 3 · Services Table -->
            <Border
                Grid.Row="3"
                Background="White"
                BorderBrush="#E4DAF5"
                BorderThickness="1"
                CornerRadius="12"
                Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Card Header -->
                    <Grid Grid.Row="0" Padding="20,16,20,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="Procedure List" FontSize="15" FontWeight="SemiBold" Foreground="#2D1554" />
                            <TextBlock Text="All services currently offered by the clinic" FontSize="12" Foreground="#9B80C4" />
                        </StackPanel>
                        <Border Grid.Column="1" Background="#EDE4F9" CornerRadius="6" Padding="10,4" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtRowCount" Text="Showing 0 services" FontSize="12" Foreground="#5B2D8E" FontWeight="SemiBold" />
                        </Border>
                    </Grid>

                    <!-- Column Headers -->
                    <Grid Grid.Row="1">
                        <Border Background="#F7F4FD" Padding="20,9">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="180" />
                                    <ColumnDefinition Width="200" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="PROCEDURE NAME" Style="{StaticResource ColHeaderStyle}" />
                                <TextBlock Grid.Column="1" Text="PRICE"          Style="{StaticResource ColHeaderStyle}" />
                                <TextBlock Grid.Column="2" Text="ADDED BY"       Style="{StaticResource ColHeaderStyle}" />
                            </Grid>
                        </Border>
                        <Border Height="1" Background="#E4DAF5" VerticalAlignment="Bottom" />
                    </Grid>

                    <!-- Data Rows -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="480">
                        <ItemsControl x:Name="ServiceListControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="local:ServiceItem">
                                    <StackPanel>
                                        <Border Padding="20,0">
                                            <Grid MinHeight="60">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="180" />
                                                    <ColumnDefinition Width="200" />
                                                </Grid.ColumnDefinitions>

                                                <!-- Procedure Name + Icon -->
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                                    <Border Width="36" Height="36" CornerRadius="8" Background="#EDE4F9">
                                                        <FontIcon Glyph="&#xE9D9;" FontSize="15" Foreground="#5B2D8E" />
                                                    </Border>
                                                    <TextBlock
                                                        Text="{x:Bind ProcedureName}"
                                                        Style="{StaticResource CellTextStyle}"
                                                        FontWeight="SemiBold" />
                                                </StackPanel>

                                                <!-- Price -->
                                                <TextBlock
                                                    Grid.Column="1"
                                                    Text="{x:Bind FormattedPrice}"
                                                    FontSize="13"
                                                    FontWeight="SemiBold"
                                                    Foreground="#2E7D32"
                                                    VerticalAlignment="Center" />

                                                <!-- Doctor -->
                                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                    <Border Width="28" Height="28" CornerRadius="14" Background="#E3F2FD">
                                                        <FontIcon Glyph="&#xE77B;" FontSize="13" Foreground="#0078D4" />
                                                    </Border>
                                                    <TextBlock
                                                        Text="{x:Bind DoctorName}"
                                                        Style="{StaticResource CellSecondaryStyle}" />
                                                </StackPanel>

                                            </Grid>
                                        </Border>
                                        <Border Height="1" Background="#F0EAF8" Margin="20,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                </Grid>
            </Border>

        </Grid>
    </ScrollViewer>
</Page>

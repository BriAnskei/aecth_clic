<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="aesth_clic.Views.Roles.Receptionist.Pages.PaymentManagement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:aesth_clic.Views.Roles.Receptionist.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F7F4FD">

    <Page.Resources>

        <local:StringToBrushConverter x:Key="StringToBrush" />

        <Style x:Key="ColHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="10" />
            <Setter Property="FontWeight"        Value="SemiBold" />
            <Setter Property="Foreground"        Value="#9B80C4" />
            <Setter Property="CharacterSpacing"  Value="50" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="CellTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="13" />
            <Setter Property="Foreground"        Value="#2D1554" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming"      Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="KebabButtonStyle" TargetType="Button">
            <Setter Property="Background"        Value="Transparent" />
            <Setter Property="BorderThickness"   Value="0" />
            <Setter Property="Padding"           Value="6,4" />
            <Setter Property="CornerRadius"      Value="6" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Padding="28,24,28,32" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- ROW 0 · Page Header -->
            <StackPanel Grid.Row="0" Spacing="2">
                <TextBlock
                    Text="Payment Management"
                    FontSize="26" FontWeight="Bold"
                    Foreground="#2D1554" />
                <TextBlock
                    Text="View and manage patient procedure payments"
                    FontSize="13" Foreground="#9B80C4" />
            </StackPanel>

            <!-- ROW 1 · Search + Filter Toolbar -->
            <Border
                Grid.Row="1"
                Background="White"
                BorderBrush="#E4DAF5"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16,12">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="180" />
                    </Grid.ColumnDefinitions>

                    <AutoSuggestBox
                        Grid.Column="0"
                        x:Name="SearchBox"
                        PlaceholderText="Search patient name or procedure…"
                        QueryIcon="Find"
                        VerticalAlignment="Center"
                        BorderBrush="#E4DAF5"
                        BorderThickness="1"
                        CornerRadius="8"
                        TextChanged="SearchBox_TextChanged" />

                    <ComboBox
                        Grid.Column="1"
                        x:Name="StatusFilter"
                        PlaceholderText="All Statuses"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        BorderBrush="#E4DAF5"
                        CornerRadius="8"
                        SelectionChanged="StatusFilter_SelectionChanged">
                        <ComboBoxItem Content="All Statuses" Tag="All"       IsSelected="True" />
                        <ComboBoxItem Content="Pending"      Tag="Pending" />
                        <ComboBoxItem Content="Completed"    Tag="Completed" />
                    </ComboBox>
                </Grid>
            </Border>

            <!-- ROW 2 · Payments Table -->
            <Border
                Grid.Row="2"
                Background="White"
                BorderBrush="#E4DAF5"
                BorderThickness="1"
                CornerRadius="12"
                Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Card Header -->
                    <Grid Grid.Row="0" Padding="20,16,20,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="Payment Records" FontSize="15" FontWeight="SemiBold" Foreground="#2D1554" />
                            <TextBlock Text="Auto-generated payments from patient procedures" FontSize="12" Foreground="#9B80C4" />
                        </StackPanel>
                        <Border Grid.Column="1" Background="#EDE4F9" CornerRadius="6" Padding="10,4" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtRowCount" Text="Showing 0 payments" FontSize="12" Foreground="#5B2D8E" FontWeight="SemiBold" />
                        </Border>
                    </Grid>

                    <!-- Column Headers -->
                    <Grid Grid.Row="1">
                        <Border Background="#F7F4FD" Padding="20,9">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="220" />
                                    <ColumnDefinition Width="130" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="PATIENT"        Style="{StaticResource ColHeaderStyle}" />
                                <TextBlock Grid.Column="1" Text="PROCEDURE"      Style="{StaticResource ColHeaderStyle}" />
                                <TextBlock Grid.Column="2" Text="AMOUNT"         Style="{StaticResource ColHeaderStyle}" />
                                <TextBlock Grid.Column="3" Text="STATUS"         Style="{StaticResource ColHeaderStyle}" />
                                <TextBlock Grid.Column="4" Text=""               Style="{StaticResource ColHeaderStyle}" />
                            </Grid>
                        </Border>
                        <Border Height="1" Background="#E4DAF5" VerticalAlignment="Bottom" />
                    </Grid>

                    <!-- Data Rows -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="520">
                        <ItemsControl x:Name="PaymentListControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="local:PaymentItem">
                                    <StackPanel>
                                        <Border Padding="20,0">
                                            <Grid MinHeight="60">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="220" />
                                                    <ColumnDefinition Width="130" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="60" />
                                                </Grid.ColumnDefinitions>

                                                <!-- Patient Name + Avatar -->
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                                    <Border
                                                        Width="36" Height="36" CornerRadius="18"
                                                        Background="{Binding AvatarColor, Converter={StaticResource StringToBrush}}">
                                                        <TextBlock
                                                            Text="{x:Bind Initials}"
                                                            FontSize="13" FontWeight="SemiBold" Foreground="White"
                                                            HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>
                                                    <TextBlock
                                                        Text="{x:Bind PatientName}"
                                                        Style="{StaticResource CellTextStyle}"
                                                        FontWeight="SemiBold" />
                                                </StackPanel>

                                                <!-- Procedure Name -->
                                                <TextBlock Grid.Column="1" Text="{x:Bind ProcedureName}" Style="{StaticResource CellTextStyle}" />

                                                <!-- Amount -->
                                                <TextBlock Grid.Column="2" Text="{x:Bind Amount}" Style="{StaticResource CellTextStyle}" FontWeight="SemiBold" />

                                                <!-- Status — text only, colored, no background -->
                                                <TextBlock
                                                    Grid.Column="3"
                                                    Text="{x:Bind Status}"
                                                    FontSize="12"
                                                    FontWeight="SemiBold"
                                                    Foreground="{Binding StatusColor, Converter={StaticResource StringToBrush}}"
                                                    VerticalAlignment="Center" />

                                                <!-- Kebab Menu -->
                                                <Button
                                                    Grid.Column="4"
                                                    Tag="{x:Bind PaymentId}"
                                                    Style="{StaticResource KebabButtonStyle}"
                                                    Click="KebabMenu_Click"
                                                    HorizontalAlignment="Center"
                                                    ToolTipService.ToolTip="More options">
                                                    <FontIcon Glyph="&#xE712;" FontSize="16" Foreground="#5B2D8E" />
                                                </Button>

                                            </Grid>
                                        </Border>
                                        <Border Height="1" Background="#F0EAF8" Margin="20,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                </Grid>
            </Border>

        </Grid>
    </ScrollViewer>
</Page>
